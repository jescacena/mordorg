{
  "name": "cercemap_geojson_source",
  "version": "1.0.0",
  "description": "GeoJSON source database for Cercemap.org",
  "main": "index.js",
  "scripts": {
    "compile": "babel src -d lib",
    "start": "npm run compile && node ./lib/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JES",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security",
    "request": "^2.79.0"
  },
  "devDependencies": {
    "axios": "^0.15.3",
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0"
  }
}
